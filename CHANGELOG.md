# Repository changelog

Chronological history of this monorepo across every plugin release. Each
heading is a per-plugin release tag -- there is no repository version number.

For what changed in a plugin you installed, read that plugin's own
`CHANGELOG.md`. This file additionally records repo-level work (CI, licensing,
release tooling) that belongs to no single plugin.

Generated by [git-cliff](https://git-cliff.org) from commit history.
## Unreleased

### Added

- **gpu-monitor:** Report GPU temperature and power draw

### Fixed

- **gpu-monitor:** Stay out of the status bar on machines with no NVIDIA GPU
- **repo:** Stop the plugin build rejecting quoted text that follows the word from

### Documentation

- **repo:** Describe the repository and its plugins on the GitHub landing page
## gpu-monitor v0.2.0 - 2026-09-09

### Added

- **gpu-monitor:** Set the VRAM level at which the status-bar chip warns

### Fixed

- **repo:** Suggest 0.1.0 instead of n/a when a plugin has no prior release
- **gpu-monitor:** Report an error instead of crashing when nvidia-smi cannot run
- **gpu-monitor:** Keep showing a GPU whose utilization reads as unavailable
- **gpu-monitor:** Stop reporting a GPU whose total VRAM reads as zero
- **gpu-monitor:** Warn at the configured VRAM level instead of just past it

### Documentation

- **repo:** Track the generated repository changelog

### Maintenance

- **repo:** Pin that a release splice preserves hand-written changelog history
- **gpu-monitor:** Release v0.2.0
## open-config v0.1.0 - 2026-08-31

### Added

- **repo:** Rebuild desktop plugin bundles from source with one command
- **open-config:** Open config.yaml and .env from the status bar, palette or /config

### Fixed

- **repo:** Keep repo-wide work out of a plugin's changelog

### Documentation

- Verify pinned installs in a throwaway HERMES_HOME
- Describe the generated changelog and local release flow

### Maintenance

- Generate changelogs from commit history and script releases
- Gate PR titles and preview the generated changelog
- **gpu-monitor:** Cover GPU sampling, subprocess failures and /gpu rendering
- **open-config:** Release v0.1.0
## gpu-monitor v0.1.0 - 2026-08-31

### Fixed

- **gpu-monitor:** Support current Hermes installer

### Documentation

- **gpu-monitor:** Make install and support docs release-ready
- Define the per-plugin release workflow

### Maintenance

- Add MIT license and keep working docs local

### Uncategorized

- First commit with gpu-monitor plugin source
