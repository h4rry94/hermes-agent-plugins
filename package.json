{
  "name": "hermes-agent-plugins",
  "private": true,
  "type": "module",
  "description": "Build tooling for the desktop halves of the plugins in this repo. Not published, not installed by Hermes - `hermes plugins install` only ever reads a single <plugin>/ folder.",
  "scripts": {
    "build": "node build.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "26.5.1",
    "@types/react": "19.3.0",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  },
  "packageManager": "pnpm@11.23.0"
}
